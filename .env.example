# IPPAN Explorer Environment Variables
# Copy this file to .env and configure for your environment

# =============================================================================
# Server-side RPC Configuration (RECOMMENDED)
# =============================================================================
# Server-only env var for the RPC gateway URL.
# This is NOT exposed to the browser and is the preferred way to configure RPC.
# The API routes (e.g., /api/rpc/status) will use this to proxy requests.
IPPAN_RPC_BASE_URL=http://188.245.97.41:8080

# =============================================================================
# Public RPC Configuration (for display only)
# =============================================================================
# These NEXT_PUBLIC_ vars are exposed to the browser but are ONLY used for
# display purposes (showing users what RPC endpoint is being used).
# 
# IMPORTANT: All actual RPC requests from the browser go through the
# /api/rpc/* proxy to avoid mixed-content issues (HTTPS -> HTTP blocked).
# The browser NEVER makes direct HTTP requests to the RPC gateway.
NEXT_PUBLIC_IPPAN_RPC_BASE=http://188.245.97.41:8080
NEXT_PUBLIC_NODE_RPC=http://188.245.97.41:8080

# P2P endpoint (optional, for network diagnostics)
NEXT_PUBLIC_NODE_P2P=http://188.245.97.41:9000

# =============================================================================
# Demo Mode (NEVER use in production)
# =============================================================================
# Set to "true" to enable mock/demo data fallback (for development only).
# Default is "false" - production should NEVER enable this.
NEXT_PUBLIC_DEMO_MODE=false

# =============================================================================
# Legacy / Back-compat Vars (auto-detected if present)
# =============================================================================
# These are supported for backwards compatibility but not recommended:
# NEXT_PUBLIC_IPPAN_RPC_URL
# NEXT_PUBLIC_IPPAN_RPC_FALLBACK
# NEXT_PUBLIC_RPC_URL
# NEXT_PUBLIC_EXPLORER_API
# VITE_NODE_RPC
# IPPAN_RPC_URL
# IPPAN_RPC_BASE
